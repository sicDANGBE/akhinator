# Solutions (3 algorithmes)

Chaque fichier expose:

```python
def decide_next_action(context: dict) -> dict:
    ...
```

Copie/colle une solution dans `server/algo/decision.py`, puis relance l'API.

Comparer via le bench:

```bash
python -m server.quality_report --algo solutions/decision_information_gain.py
```
